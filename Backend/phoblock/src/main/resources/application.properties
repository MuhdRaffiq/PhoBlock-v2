# Local Server MySql Configuration
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/phoblock_localtest?verifyServerCertificate=false&useSSL=false&requireSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# user for MySql Local Server
spring.datasource.username=root

# password to MySql Local Server
spring.datasource.password=Mysqlroot

# Server Mysql Configuration
# spring.datasource.url=jdbc:mysql://coms-309-vb-05.cs.iastate.edu:3306/Hackathon_Phoblock?verifyServerCertificate=false&useSSL=false&requireSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# user for mysql server VB_05
# spring.datasource.username=phoblock

# password to mysql server
# spring.datasource.password=phoblock2020

# this is the port your web server will run on
server.port=8080

# Hibernate SQL Dialect
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# JPA
# This tells jdbc to create the tabls if they do not already exist
# Defaults to none, in which case you must make the tables to match the Entity owners
# Run the 'src/main/resources/db/mysql/data.sql' script in MySql workbench or CLI to insert dummy data
spring.jpa.hibernate.ddl-auto=create

spring.jpa.show-sql=true
logging.file=application.log

# Logging
logging.level.org.springframework=DEBUG

# server.forward-headers-strategy=FRAMEWORK
